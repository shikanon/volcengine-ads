- [x] 已创建变更目录 `.trae/specs/integrate-feishu-video-download/`，并包含 `spec.md`、`tasks.md`、`checklist.md`
- [x] 规格明确要求将飞书视频下载封装为主进程可复用方法，而不是继续停留在临时脚本
- [x] 规格明确要求通过现有 `task:create -> worker -> pipeline` 主链路集成新任务类型
- [x] 规格明确要求新增应用内最小入口，而不是只有内部调用接口
- [x] 任务清单覆盖输入契约、主进程封装、任务接入、页面入口和测试验证
- [x] 任务依赖关系明确，且未把范围扩大为不必要的完整素材管理模块
- [x] 规格要求保留浏览器会话复用、清晰度降级和有限重试等已验证成功的关键策略
- [x] 规格要求对登录态失效、非法输入和下载结果汇总做显式处理
